#include <stdio.h>

#define GLORKUS 16

int main()
{
    int blorf;

    blorf = 22;

    printf("La valeur de blorf est: %d\n", blorf);
    printf("Le résultat de blorf plus GLORKUS est: %d\n", blorf+GLORKUS);
    printf("La valeur de blorf au carré est: %d\n", blorf*blorf);

    return(0);
}
